{
  "name": "web-chess",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Starting build...' && tsc && postcss src/css/* --dir dist/css/ && npm run copy-html && echo 'Build complete.'",
    "watch": "tsc --watch",
    "copy-html": "copyfiles -u 1 src/**/*.html dist"
  },
  "keywords": [],
  "author": "Daniel-De-Dev",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "copyfiles": "^2.4.1",
    "cssnano": "^7.0.1",
    "postcss": "^8.4.38",
    "postcss-cli": "^11.0.0"
  }
}
